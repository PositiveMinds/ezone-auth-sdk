name: ezone_sdk
description: ezone passwordless authentication SDK for Dart and Flutter
version: 0.1.0
homepage: https://github.com/ezone-sdk/ezone-sdk

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  pointycastle: ^3.7.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0
  http_mock_adapter: ^0.6.0
